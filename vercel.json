{
  "version": 2,
  "builds": [
    {
      "src": "api/smsapi.py",
      "use": "@now/python",
      "config": { "maxLambdaSize": "15mb" }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "api/smsapi.py" }
  ]
}
